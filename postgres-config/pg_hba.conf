# TYPE  DATABASE  USER      ADDRESS        METHOD
host    all       all       0.0.0.0/0      scram-sha-256  # Основной метод
host    all       all       0.0.0.0/0      md5            # Фолбэк для совместимости
local   all       all                      trust